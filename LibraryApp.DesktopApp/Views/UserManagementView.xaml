<UserControl x:Class="LibraryApp.DesktopApp.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Height="274" Width="596">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- حقول الإدخال للإضافة -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,-17,0" VerticalAlignment="Top">
            <StackPanel Margin="0,0,8,0">
                <TextBlock Text="Username:"/>
                <TextBox Width="120" Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Margin="0,0,8,0">
                <TextBlock Text="Password:"/>
                <TextBox Width="120" Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Margin="0,0,8,0">
                <TextBlock Text="Phone:"/>
                <TextBox Width="120" Text="{Binding NewPhone, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="TypeId:"/>
                <TextBox Width="60" Text="{Binding NewTypeId, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <Button Content="إضافة" 
                Command="{Binding AddUserCommand}" 
                IsEnabled="{Binding AddUserCommand.CanExecute, Mode=OneWay}" Background="#FF51C54E" Height="18" Width="70" VerticalAlignment="Bottom" 
                    />
        </StackPanel>

        <!-- أزرار الإضافة والحذف مع رسالة -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,37,0,142">
            <Button Content="حذف" 
                    Command="{Binding DeleteUserCommand}" 
                    IsEnabled="{Binding DeleteUserCommand.CanExecute, Mode=OneWay}" Background="#FFEA4736" />
            <TextBlock Text="{Binding Message}" Foreground="green" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- قائمة المستخدمين -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Users}"
                 SelectedItem="{Binding SelectedUser}"
                 DisplayMemberPath="Username" Margin="0,65,0,0"/>

        <!-- زر التحديث -->
        <Button Grid.Row="3" Content="تحديث" Command="{Binding LoadUsersCommand}" HorizontalAlignment="Left" Background="#FF67B3AE"/>
    </Grid>
</UserControl>
